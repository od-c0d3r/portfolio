import styled from 'styled-components'

const LogoContainer = styled.span`
    font-family: 'Ruge Boogie', cursive;
    font-size: 50px;
    display: block;
    position: fixed;
`

const Logo = () => <LogoContainer>OmarRashaD</LogoContainer>;

export default Logo;
